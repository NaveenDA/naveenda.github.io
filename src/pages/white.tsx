import React, { useEffect, useRef } from "react";
import { gsap } from "gsap";
import "../styles/white-bg.css";
import EarthCanvas from "../earth";

const White = () => {

    return (
    <div className="white-container">
    </div>
  );
};

export default White;
